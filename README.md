# BTMesh_HostADVProvisioner

NCP Provisionner meant to provision vendor specific devices to a unique group acting as a huge broadcast subnet
The model configuration is located in app.h
The group address is fixed to 0xC001
The application and network keys are generated randomly


## Disclaimer ##

The Gecko SDK suite supports development with Silicon Labs IoT SoC and module devices. Unless otherwise specified in the specific directory, all examples are considered to be EXPERIMENTAL QUALITY which implies that the code provided in the repos has not been formally tested and is provided as-is.  It is not suitable for production environments.  In addition, this code will not be maintained and there may be no bug maintenance planned for these resources. Silicon Labs may update projects from time to time.